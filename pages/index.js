import React, {Component} from 'react';
import Layout from '../components/Layout';

export default props => {
    console.log('INDEX');
    return (
        <Layout>
            <h1>Recovery.org</h1>
        </Layout>
    );
};
